apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gc-argo-application
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/karthikushkala/argocd-develop.git
    targetRevision: HEAD
    path: reports
  destination: 
    server: https://kubernetes.default.svc
    namespace: gc

  syncPolicy:
    syncOptions:
    - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true